apiVersion: v2
name: prometheus
description: kube-prometheus-stack — Prometheus + Alertmanager + Grafana with Thanos sidecar
type: application
version: 0.1.0
appVersion: "0.72.0"

dependencies:
  - name: kube-prometheus-stack
    version: "57.2.0"
    repository: https://prometheus-community.github.io/helm-charts
